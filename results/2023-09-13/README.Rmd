---
title: "Untitled"
author: "Mar Llaberia-Robledillo & J. Ignacio Lucas Lledó"
date: "`r Sys.Date()`"
output:
   html_document:
      toc: TRUE
---

```{bash LISTNAMES}
FISHDATA=../../data/Fish_clean2.tsv

for POP in C.acrinasus C.aplinus C.confusus C.duplex C.fatioi C.profundus C.steinmanni D L P; do 
  #if [ ! -e C.${POP} ]; then
    gawk '($19 == "${POP}"){print $1}' $FISHDATA > ${POP}.txt
  #fi 
done

if [ ! -d FST ]; then mkdir FST; fi

if [ ! -e dirty.recode.vcf ]; then
  vcftools --vcf $VCF \
           --out dirty \
           --remove ../2022-05-04/TheWorst.txt \
           --recode \
           --recode-INFO-all > vcftools.log 2>&1 
fi

```


```{bash}

if [ ! -e FST/Thun-Brienz.windowed.weir.fst ]; then
   vcftools --vcf dirty.recode.vcf \
            --out FST/Thun-Brienz \
            --thin 500 \
            --remove-indels \
            --mac 2 \
            --max-alleles 2 \
            --max-missing 1 \
            --max-meanDP 1000 \
            --weir-fst-pop C.fatioi.txt \
            --weir-fst-pop C.steinmanni.txt \
            --fst-window-size 1000000 \
            --fst-window-step  250000 >> vcftools.log 2>&1
fi

```

